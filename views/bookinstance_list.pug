extends layout

block content
    h1(class='title') Book-instance list

    div(class='container')
        table(class='table')
            thead(style='background-color: #a29bfe')
                tr(style='color: #fff')
                    th Books
                    th Author 
                    th Actions
            tbody(style='font-size: 13px')
                each val in bookinstance_list
                    tr(class='t-hover') 
                        td #{val.book.title} 
                        td #{val.imprint} 
                            if val.status=='Available' 
                                span.text-success #{val.status}
                            else if val.status=='Maintenance' 
                                span.text-danger #{val.status}
                            else
                                span.text-warning #{val.status}
                            if val.status!='Available' 
                                span  (Due: #{val.due_back_formated} )
                        td 
                            a(href=val.url)
                                i(class='icono fas fa-list' style='padding: 6px; color: #78C2AD')
                            a(href=val.url+'/update')
                                i(class='icono fas fa-edit' style='padding: 6px; color: #78C2AD')
                            a(href=val.url+'/delete')
                                i(class='icono fas fa-trash-alt' style='padding: 6px; color: #78C2AD')
                               
                                
                else
                    li There are no book copies in this library.       